services:
  db:
    image: pgvector/pgvector:pg16
    container_name: realfood-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: realfood
      POSTGRES_PASSWORD: realfood_local
      POSTGRES_DB: fuelgood
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
